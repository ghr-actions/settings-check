on: push

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        env:
          GHT: ${{ secrets.PAT_TOKEN }}
        with:
          token: PAT_TOKEN
          repositories: GavinF17/GavinF17.github.io
